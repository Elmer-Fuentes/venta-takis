import {useEffect,useState} from 'react';import type{GalleryImage}from'../types';import{db}from'../services/storage';
export default function Gallery(){const[images,setImages]=useState<GalleryImage[]>([]);useEffect(()=>setImages(db.images()),[]);return <section className="section page"><div className="section-title"><span>Galería</span><h1>Se ve tan bueno como sabe</h1><p>Las imágenes agregadas desde el panel administrativo aparecen aquí.</p></div><div className="gallery">{images.map(i=><figure key={i.id}><img src={i.src} alt={i.alt}/><figcaption>{i.alt}</figcaption></figure>)}</div></section>}
