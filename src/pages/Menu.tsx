import {useMemo,useState} from 'react';
import {Plus,Minus,ShoppingCart} from 'lucide-react';
const products=[{id:'p1',name:'Pepino Loco',description:'Pepino fresco, limón, Tajín, chamoy y toppings a elección.',price:20,emoji:'🥒'},{id:'p2',name:'Mango Loco',description:'Mango fresco, chamoy, Tajín, limón y toppings a elección.',price:25,emoji:'🥭'},{id:'p3',name:'Dúo Loco',description:'Mitad pepino, mitad mango, con mezcla completa de toppings.',price:30,emoji:'🔥'}];
export default function Menu(){const [cart,setCart]=useState<Record<string,number>>({});const total=useMemo(()=>products.reduce((s,p)=>s+(cart[p.id]||0)*p.price,0),[cart]);const set=(id:string,d:number)=>setCart(c=>({...c,[id]:Math.max(0,(c[id]||0)+d)}));return <section className="section page"><div className="section-title"><span>Menú</span><h1>Elige tu locura</h1><p>Precios demostrativos. Puedes modificarlos fácilmente en el código.</p></div><div className="product-grid">{products.map(p=><article className="product-card" key={p.id}><div className="product-emoji">{p.emoji}</div><h2>{p.name}</h2><p>{p.description}</p><strong>Q{p.price.toFixed(2)}</strong><div className="counter"><button onClick={()=>set(p.id,-1)}><Minus/></button><b>{cart[p.id]||0}</b><button onClick={()=>set(p.id,1)}><Plus/></button></div></article>)}</div><aside className="cart-bar"><div><ShoppingCart/><span>Total de demostración</span></div><strong>Q{total.toFixed(2)}</strong></aside></section>}
